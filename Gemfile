source "https://rubygems.org"

gem "github-pages", "~> 231", group: :jekyll_plugins
gem "jekyll-include-cache", group: :jekyll_plugins
gem "webrick", "~> 1.8"

# Ruby 3.4 dropped these from the default bundled gems; old Jekyll/github-pages
# versions still need them but never declared the dependency explicitly.
gem "csv"
gem "logger"
gem "base64"
gem "bigdecimal"
